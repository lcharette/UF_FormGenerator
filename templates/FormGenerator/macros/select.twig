{% macro generate(input) %}
    <select{% for type,value in input %} {{type}}="{{value}}"{% endfor %}>
        {% for option, label in input.options %}<option value="{{option}}" {% if (option == input.data) %}selected{% endif %}>{{translate(label)}}</option>{% endfor %}
    </select>
{% endmacro %}